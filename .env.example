# Desarrollo: se deja vacío. El proxy de Vite reenvía /services/apis y /healthz
# al hub, así que el navegador nunca hace una petición cruzada y no hace falta
# CORS en el backend.
VITE_API_BASE_URL=

# A dónde reenvía ese proxy. El compose del hub publica el 8001.
VITE_API_PROXY_TARGET=http://localhost:8001
